None of the three algorithms can work with directed graphs nor with edges with negative weights
However, for the given graph , since all the nodes are reachable , it is possible to implement these algorithms.
